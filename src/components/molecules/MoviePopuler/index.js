import React from 'react';
import {StyleSheet, Text, View} from 'react-native';

const MoviePopular = () => {
  return (
    <View>
      <Text>Movie Popular</Text>
    </View>
  );
};

export default MoviePopular;

const styles = StyleSheet.create({});
